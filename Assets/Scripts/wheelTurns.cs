using UnityEngine;
using System.Collections;

public class wheelTurns : MonoBehaviour {

	public float angle;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//float rot = getAngles ();
		//transform.Rotate (Vector3.up, rot, Space.World);
	}

	//float getAngles() { 
		//GameObject go = GameObject.Find ("Car_root");
		//angle = go.GetComponent<SimpleCarController>().getAngle();
		//return angle;
	//}
}
